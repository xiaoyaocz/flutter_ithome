import 'package:flutter/material.dart';

class NewsRankView extends StatelessWidget {
  const NewsRankView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [
          Row(
            children: [],
          ),
        ],
      ),
    );
  }
}
